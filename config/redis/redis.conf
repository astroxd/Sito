# Network & Ports
bind 0.0.0.0
port 6379

# Persistenza (Save su disco ogni 20 sec se almeno 1 chiave è cambiata)
save 20 1
dir /data

# Logging
loglevel warning

# Includi il file delle ACL per utenti e password
aclfile /usr/local/etc/redis/users.acl